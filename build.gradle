
group = 'org.xbib.gradle.plugin'
version = '1.1.0'

apply plugin: 'groovy'
apply plugin: 'maven'

buildscript {
  repositories {
    jcenter()
  }
}

repositories {
  mavenLocal()
  mavenCentral()
}

configurations {
  wagon
}

dependencies {
  compile gradleApi()
  compile 'org.xbib:jacc:2.0.0-SNAPSHOT'
  wagon 'org.apache.maven.wagon:wagon-ssh-external:2.10'
}

uploadArchives {
  repositories {
    if (project.hasProperty("xbibUsername")) {
      mavenDeployer {
        configuration = configurations.wagon
        repository(
                id: 'xbib.org',
                url: uri('scpexe://xbib.org/repository'),
                authentication: [userName: xbibUsername, privateKey: xbibPrivateKey]
        )
      }
    }
  }
}